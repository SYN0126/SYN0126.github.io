# 博客项目约定

## 项目定位

- 这是小龙的中文个人博客，使用 Hugo 构建并部署到 GitHub Pages。
- 视觉方向保持极简、安静、以文字阅读为主，不引入不必要的框架或复杂交互。
- 网站正文与界面默认使用简体中文。

## 内容规范

- 正式文章放在 `content/posts/`，文件名格式为 `YYYY-MM-DD-英文短名.md`。
- 文章头部使用 YAML，包含 `title`、`slug`、`date`、`description` 和 `draft`。
- 正文只有一个页面标题；正文内第一个小标题从 `##` 开始。
- 新文章默认 `draft: true`，只有用户明确决定发布时才改为 `false`。
- 图片放在 `static/images/`，文章中使用 `/images/文件名` 引用。

## 开发与发布

- 日常管理统一使用根目录的 `博客管理.command`，不要再创建单独的发布入口。
- 不手动编辑或提交 `public/`；它是 Hugo 的生成结果。
- 修改模板、配置或样式后，使用带 `--panicOnWarning` 的严格构建进行验证。
- 未经用户明确要求，不执行 Git 提交、推送或线上发布。
- 保持现有 Hugo 架构和原生 HTML/CSS，除非需求确实需要，不增加前端依赖。

## 页面分工

- 首页展示最近 10 篇文章。
- 归档页展示全部文章并按年份分组。
- 未实际使用标签和分类前，不生成对应页面。

# 博客速查卡片

## 博客管理（推荐）

- Mac：双击 `博客管理.command`。
- Windows：双击 `博客管理-Windows.bat`。

两个版本的功能一致：可以新建、查看、编辑、隐藏、删除、预览和发布文章，也可以直接编辑“关于”页。Windows 目录里的 `scripts/blog-manager-windows.ps1` 是内部程序，不需要手动打开。

- 新文章默认是草稿，不会直接出现在网站上。
- “删除文章”会先移到 Mac 废纸篓或 Windows 回收站，可以恢复。
- 修改、隐藏或删除文章后，都要执行“一键发布”才会更新线上网站。

## 一键发布（推荐）

1. 打开博客管理工具。
2. 选择“8. 一键发布到 GitHub”。
3. 确认本次修改，并输入更新说明；直接回车会自动生成说明。
4. 等待出现“发布完成”。

发布功能已经包含在博客管理工具中，不再需要单独的发布文件。工具会先测试网站能否正常构建，再依次执行 Git 提交和推送。

## 换电脑后恢复

先安装 Git 和 Hugo，再克隆仓库：

```bash
git clone https://github.com/SYN0126/SYN0126.github.io.git
```

克隆完成后，Mac 双击 `博客管理.command`，Windows 双击 `博客管理-Windows.bat`。只有已经推送到 GitHub 的内容才能恢复，因此重要修改后记得执行“一键发布”。

## 新建文章

在管理工具中选择“新建文章”，按提示填写即可，不需要输入命令或手写固定格式：

1. 中文标题，例如“我的效率方法”。
2. 一句话简介；不需要时直接回车。
3. 英文简称，例如 `my-workflow`；直接回车会自动生成。
4. 标签，例如 `#思考 #效率`；直接回车就是“未分类”。

只有标题必填，其他项目都可以直接回车。日期、文件名和草稿状态都会自动生成。写完后，在管理工具中把文章设为“已发布”，再执行“一键发布”。

## 标签

标签写在文章顶部：

```yaml
tags: ["思考", "效率"]
```

- 没有填写标签时，网站会显示为“未分类”。
- 每篇文章可以有一个或多个标签。
- 博客顶部的“标签”页会按标签列出文章。
- 修改标签后照常执行“一键发布”即可。

## 文章头部模板

```yaml
---
title: "文章标题"
slug: "english-slug"
description: "一句话简介"
tags: ["未分类"]
draft: false
---

正文写这里，正文里的第一个小标题从 ## 开始。
```

## 图片

- 放到 `static/images/` 
- 文章里引用：`![图片说明](/images/xxx.png)`

## “关于”页

在管理工具中选择“编辑‘关于’页”，或直接编辑 `content/about.md`。

如果要放照片：

1. 把照片放到 `static/images/`，例如 `avatar.jpg`。
2. 在 `content/about.md` 中加入 `![小龙的照片](/images/avatar.jpg)`。
3. 照常执行“一键发布”。

## 本地预览

```bash
cd ~/Desktop/Blog && hugo server -D
# 浏览器打开 http://localhost:1313
```

## 手动发布

`~` 表示当前用户的主目录；`~/Desktop/Blog` 表示桌面的 Blog 文件夹。

```bash
cd ~/Desktop/Blog
git add -A
git commit -m "新文章：xxx"
git push origin main
```

- `git add -A`：把新增、修改和删除的文件加入本次提交。
- `git commit -m "说明"`：把变化保存到本地 Git 仓库。
- `git push origin main`：把本地提交推送到 GitHub，随后自动部署网站。
- 不建议使用 `git commit -am "说明"`，因为它不会包含新文章或新图片。

# 博客速查卡片

## 一键发布（推荐）

1. 打开桌面的 `Blog` 文件夹。
2. 双击 `发布博客.command`。
3. 确认本次修改，并输入更新说明；直接回车会自动生成说明。
4. 等待出现“发布完成”。

脚本会先测试网站能否正常构建，再依次执行 Git 提交和推送。

## 新建文章

```bash
cd ~/Desktop/Blog
hugo new posts/YYYY-MM-DD-英文短名.md
```

## 文章头部模板

```yaml
---
title: "文章标题"
slug: "english-slug"
description: "一句话简介"
draft: false
---

正文写这里，正文里的第一个小标题从 ## 开始。
```

## 图片

- 放到 `static/images/` 
- 文章里引用：`![图片说明](/images/xxx.png)`

## 本地预览

```bash
cd ~/Desktop/Blog && hugo server -D
# 浏览器打开 http://localhost:1313
```

## 手动发布

`~` 表示当前用户的主目录；`~/Desktop/Blog` 表示桌面的 Blog 文件夹。

```bash
cd ~/Desktop/Blog
git add -A
git commit -m "新文章：xxx"
git push origin main
```

- `git add -A`：把新增、修改和删除的文件加入本次提交。
- `git commit -m "说明"`：把变化保存到本地 Git 仓库。
- `git push origin main`：把本地提交推送到 GitHub，随后自动部署网站。
- 不建议使用 `git commit -am "说明"`，因为它不会包含新文章或新图片。
